import jorge.*

describe "group of tests for pepita" {

  test "jorge existe y su alegria inicial es 10" {
    assert.equals(10, jorge.alegria())
  }

  test "boca gana y jorge se alegra" {
    boca.gana()
    assert.equals(11, jorge.alegria())
  }
}